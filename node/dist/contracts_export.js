// Generated by CoffeeScript 1.10.0
(function() {
  var _, c, config_path, contracts, ctr, env, fs, i, interface_path, len;

  env = require('./env');

  _ = require('underscore');

  fs = require('fs');

  c = console;

  contracts = env.contracts;

  for (i = 0, len = contracts.length; i < len; i++) {
    ctr = contracts[i];
    delete ctr.instance;
    delete ctr.compiled;
    ctr;
  }

  config_path = (process.cwd()) + "/../config";

  interface_path = config_path + "/contracts_interface.json";

  c.log("out:", interface_path);

  fs.writeFileSync(interface_path, JSON.stringify(contracts));

  process.exit(0);

}).call(this);
