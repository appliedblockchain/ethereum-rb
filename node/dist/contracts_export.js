// Generated by CoffeeScript 1.10.0
(function() {
  var _, babi_path, c, config_path, contracts, ctr, env, fs, i, len;

  env = require('./env');

  _ = require('underscore');

  fs = require('fs');

  c = console;

  contracts = env.contracts;

  for (i = 0, len = contracts.length; i < len; i++) {
    ctr = contracts[i];
    delete ctr.instance;
    delete ctr.compiled;
    ctr;
  }

  c.log("\nContracts:");

  c.log((JSON.stringify(contracts)) + "\n");

  config_path = (process.cwd()) + "/../config";

  babi_path = config_path + "/contracts_babi.json";

  c.log("out:", babi_path);

  fs.writeFileSync(babi_path, JSON.stringify(contracts));

  process.exit(0);

}).call(this);
