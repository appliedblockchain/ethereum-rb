// Generated by CoffeeScript 1.10.0
(function() {
  var _, c, coinbase, contracts, deasync, deploy, deployContract, env, eth, fs;

  env = require('./env');

  deploy = require('./deploy');

  _ = require('underscore');

  fs = require('fs');

  deasync = require('deasync');

  c = console;

  eth = env.eth;

  coinbase = env.getCoinbase();

  contracts = env.contracts;

  deployContract = function(coinbase, contract) {
    var res;
    res = null;
    deploy.deployContract(coinbase, contract, function(contractData) {
      return res = contractData;
    });
    while (res === null) {
      deasync.runLoopOnce();
    }
    return res;
  };

  contracts.forEach(function(contract, idx) {
    return setTimeout(function() {
      return deployContract(coinbase, contract, function() {
        c.log("contract deployed: " + result.name);
        c.log("getters: ", JSON.stringify(result.getters));
        c.log("setters: ", JSON.stringify(result.setters));
        c.log("");
        return c.log("done!");
      });
    }, idx * 3000);
  });

}).call(this);
